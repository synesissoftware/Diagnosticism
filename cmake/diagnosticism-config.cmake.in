@PACKAGE_INIT@

# Diagnosticism::core is a standalone C library with no transitive package
# dependencies. Do not add find_dependency() here unless a PUBLIC/INTERFACE
# link requirement is intentionally introduced on an exported target.

include("${CMAKE_CURRENT_LIST_DIR}/@EXPORT_NAME@-targets.cmake")
